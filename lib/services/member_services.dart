import 'package:community_knowledgebase/models/member.dart';

class MemberServices {
  Future<Member> addMember(Member memberInfo) async {
    return memberInfo;
  }

  Future<Member> readMember(String userId) async {
    Member memberInfo = Member();

    return memberInfo;
  }
}
